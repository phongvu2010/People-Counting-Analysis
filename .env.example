# ===================================================================
# CẤU HÌNH CHUNG CỦA ỨNG DỤNG
# ===================================================================
PROJECT_NAME="Analytics iCount People API"
DESCRIPTION="API cung cấp dữ liệu phân tích lượt ra vào cửa hàng."

# Token bí mật để bảo vệ các API nội bộ (ví dụ: /admin/clear-cache).
# NÊN THAY ĐỔI thành một chuỗi ngẫu nhiên, dài và khó đoán trong môi trường production.
INTERNAL_API_TOKEN=""

# Cấu hình CORS (Cross-Origin Resource Sharing)
# Danh sách các địa chỉ frontend được phép gọi API này (cách nhau bằng dấu phẩy).
BACKEND_CORS_ORIGINS=["http://127.0.0.1", "http://127.0.0.1:8000", "http://localhost", "http://localhost:8000"]


# ===================================================================
# CẤU HÌNH LOGIC NGHIỆP VỤ (BUSINESS LOGIC)
# ===================================================================
# Ngưỡng để xác định một giá trị là bất thường (outlier).
# Ví dụ: Nếu trong một bản ghi, số lượt vào > 100 thì coi là bất thường.
OUTLIER_THRESHOLD=100

# Tỷ lệ để điều chỉnh giá trị bất thường.
# Ví dụ: 0.00001 sẽ nhân giá trị bất thường với số này. Đặt là 0 để thay bằng 1.
OUTLIER_SCALE_RATIO=0.00001

# Định nghĩa "ngày làm việc", ví dụ từ 9h sáng hôm nay đến 2h sáng hôm sau.
# Giúp nhóm dữ liệu theo ca làm việc thay vì ngày dương lịch thuần túy.
WORKING_HOUR_START=9
WORKING_HOUR_END=2


# ===================================================================
# CẤU HÌNH KẾT NỐI DATABASE (MS SQL SERVER)
# ===================================================================
# Thay thế các giá trị your_* bằng thông tin thực tế của bạn.
SQLSERVER_DRIVER="FreeTDS"
SQLSERVER_SERVER="your_server_address"
SQLSERVER_DATABASE="your_database_name"
SQLSERVER_UID="your_username"
SQLSERVER_PWD="your_password"


# ===================================================================
# CẤU HÌNH TÙY CHỌN (OPTIONAL)
# ===================================================================
# Ghi đè log level để debug. Các giá trị hợp lệ: DEBUG, INFO, WARNING, ERROR.
# Bỏ dấu # để kích hoạt.
# LOG_LEVEL=DEBUG

# Tự động xóa các file Parquet tạm nếu quy trình ETL thất bại.
# Đặt là 'false' nếu bạn muốn giữ lại file để gỡ lỗi.
ETL_CLEANUP_ON_FAILURE=true
